// Escreva uma função que recebe 2 números e retorne o maior deles

function maiorNumero(x, y){
    return x > y ? x : y;
}

// console.log(maiorNumero(150, 100));

const max = (x, y) => x > y ? x : y;

console.log(max(25, 15));